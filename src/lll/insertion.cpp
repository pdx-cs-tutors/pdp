#include "../../include/lll.h"

namespace lll {
    // Insert the value at the end of the list
    // Return the number of values inserted
    int append(node*& head, int value) {
        return 0;
    }

    // Insert the value at the end of the list,
    //   but only if the list doesn't already contain the value
    // Return the number of values inserted
    int append_unique(node*& head, int value) {
        return 0;
    }

    // Insert the value at the beginning of the list
    // Return the number of values inserted
    int prepend(node*& head, int value) {
        return 0;
    }

    // Insert the value at the beginning of the list,
    //   but only if the list doesn't already contain the value
    // Return the number of values inserted
    int prepend_unique(node*& head, int value) {
        return 0;
    }

    // Insert the value in sorted order (smallest to largest)
    // Return the number of values inserted
    int insert_ordered(node*& head, int value) {
        return 0;
    }
}
