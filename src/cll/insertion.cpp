#include "../../include/cll.h"

namespace cll {
    // Insert the value at the end of the list
    // Return the number of values inserted
    int append(node*& rear, int value) {
        return 0;
    }

    // Insert the value at the end of the list,
    //   but only if the value is not already present
    // Return the number of values inserted
    int append_unique(node*& rear, int value) {
        return 0;
    }

    // Insert the value at the beginning of the list
    // Return the number of values inserted
    int prepend(node*& rear, int value) {
        return 0;
    }

    // Insert the value at the beginning of the list,
    //    but only if the value is not already present
    // Return the number of values inserted
    int prepend_unique(node*& rear, int value) {
        return 0;
    }

    // Insert the value in sorted order (smallest to largest)
    // Return the number of values inserted
    int insert_ordered(node*& rear, int value) {
        return 0;
    }
}
