#include "../../include/cll.h"

namespace cll {
    // Remove the first occurrence of the value
    // Return the number of values removed
    int remove(node*& rear, int value) {
        return 0;
    }

    // Remove all occurrences of the value
    // Return the number of values removed
    int remove_all(node*& rear, int value) {
        return 0;
    }

    // Remove all even values
    // Return the number of values removed
    int remove_evens(node*& rear) {
        return 0;
    }

    // Destroy the entire list, deallocating all memory
    // Return the number of values inserted
    int destroy(node*& rear) {
        return 0;
    }
}
